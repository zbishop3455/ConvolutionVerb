
#pragma once


/**
 * Wrapper around juce::Convolution class to handle impulse response modification.
 * 
 * Decay and PreDelay parameters are implemented by modifying the impulse response.
 */
class ConvolutionProcessor {
public:
    ConvolutionProcessor();
    ~ConvolutionProcessor();
private:

};


